import { Injectable } from '@nestjs/common';
import { Model } from 'mongoose';
import { InjectModel } from '@nestjs/mongoose';
import { {{className}}, {{className}}Document } from './schemas/{{pathName}}.schema';

@Injectable()
export class {{className}}Repository {
  constructor(
    @InjectModel({{className}}.name) private readonly {{variableName}}Model: Model<{{className}}Document>,
  ) {}

  async create(input: Partial<{{className}}Document>): Promise<{{className}}> {
    return this.{{variableName}}Model.create(input);
  }

  async findOneById(id: string): Promise<{{className}} | null> {
    return this.{{variableName}}Model.findById(id);
  }
}
