import { ValidationError } from '../../common/errors/business.error';
export const {{toFullUpperCase moduleName}}_DOES_NOT_EXIST_ERROR_CODE = '{{toFullUpperCase moduleName}}_NOT_EXISTS_ERROR_CODE';

export class {{toUpperCase moduleName}}NotFoundError extends ValidationError {
  constructor(details?: unknown) {
    super(
      'Entity with provided data does not exists',
      {{toFullUpperCase moduleName}}_DOES_NOT_EXIST_ERROR_CODE,
      details,
    );
  }
}
