import { ValidationError } from '../../common/errors/business.error';
export const {{errorName}}_DOES_NOT_EXIST_ERROR_CODE = '{{errorName}}_NOT_EXISTS_ERROR_CODE';

export class {{className}}NotFoundError extends ValidationError {
  constructor(details?: unknown) {
    super(
      'Entity with provided data does not exists',
      {{errorName}}_DOES_NOT_EXIST_ERROR_CODE,
      details,
    );
  }
}
