import { Module } from '@nestjs/common';
import { {{className}}Service } from './{{pathName}}.service';
import { {{className}}Repository } from './{{pathName}}.repository';
import { {{className}}, {{className}}Schema } from './schemas/{{pathName}}.schema';
import { MongooseModule } from '@nestjs/mongoose';
import { {{className}}Resolver } from './{{pathName}}.resolver';

@Module({
imports: [
    MongooseModule.forFeature([{ name: {{className}}.name, schema: {{className}}Schema }]),
    ],
    providers: [{{className}}Service, {{className}}Repository, {{className}}Resolver],
    exports: [{{className}}Service],
})
export class {{className}}Module {}