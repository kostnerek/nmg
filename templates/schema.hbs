import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument, Types } from 'mongoose';

export type {{toUpperCase moduleName}}Document = HydratedDocument<{{toUpperCase moduleName}}>;

@Schema({ timestamps: true })
export class {{toUpperCase moduleName}} {
  _id: Types.ObjectId;

  @Prop({ type: String })
  name: string;
}

export const {{toUpperCase moduleName}}Schema = SchemaFactory.createForClass({{toUpperCase moduleName}});
