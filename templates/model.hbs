import { Field, ObjectType } from '@nestjs/graphql';
import { {{className}}Document } from '../schemas/{{pathName}}.schema';

@ObjectType('{{class}}')
export class {{className}}Model {
  @Field(()=>String)
  id: string;

  constructor(input: {{className}}Document | {{className}}Model) {
    Object.assign(this, {
      ...((input as any)._doc ? (input as {{className}}Document).toObject({ virtuals: true }) : input),
    });
  }
}
